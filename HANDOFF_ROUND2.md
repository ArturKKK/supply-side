# Передача контекста, раунд 2: стикеры и ликвидность вне Steam

Продолжение `HANDOFF.md`. Репозиторий тот же: `github.com/ArturKKK/supply-side`.
Статус на 2026-08-09 19:09, два сборщика ещё работают.

## 0. Что изменилось в постановке

Раунд 1 дал карту по кейсам и капсулам. Раунд 2 — приоритет на **стикеры**, и по
ходу вскрылась методическая ошибка, из-за которой предыдущий вывод пришлось
пересобрать. Об этом пункт 4, он главный.

## 1. Капсулы и стикеры разведены на разные классы

Это разные активы: капсула — лотерея над набором, стикер — конкретный предмет.
И горят они по-разному: капсулу можно только уничтожить открытием, стикер
создаётся открытием капсулы и уничтожается наклейкой на оружие.

`stickers.py` разбирает `market_hash_name` по структуре
`Sticker | <субъект> (<вариант>) | <турнир>`. Всё берётся из самой строки:
вариант (Paper / Foil / Holo / Gold / Glitter), субъект, турнир, год.
Локализованные (русские, чешские) архивные URL отбрасываются, а не угадываются.

Разобрано **5 491 стикер** из 6 821 архивных имён.

| вариант | всего в архиве |
|---|---|
| Paper | 2 204 |
| Foil | 1 177 |
| Gold | 950 |
| Holo | 817 |
| Glitter | 343 |

## 2. Отбор целей: сетка, а не «топ по обороту»

В задании было «топ-300 по обороту». Так нельзя: оборот становится известен
только **после** загрузки истории, поэтому он не может управлять загрузкой.
Вместо этого — сбалансированная сетка: для каждого турнира берутся субъекты,
существующие в наибольшем числе вариантов, и все их варианты целиком. Это даёт
корректное сравнение Paper vs Holo vs Gold **внутри одной команды и одного
турнира**, а не набор разрозненных предметов.

304 цели: Paper 88, Holo 67, Foil 71, Gold 63, Glitter 15. Старым мейджорам
(Krakow 2017, Boston 2018, Atlanta 2017, Katowice 2019, London 2018, Berlin 2019,
Cologne 2016, MLG Columbus 2016) выделено больше субъектов, чем свежим.

## 3. Стадия жизненного цикла — построена, и она содержательна

Возраст = лет с турнира, год берётся из имени. Отдельная оговорка в коде: год
турнира ≠ год закрытия раздачи, поэтому `age` — нижняя граница.

**Капсулы:**

| возраст | n | медиана цены | vol 30d | оборот г/г | CAGR Q1 | CAGR медиана | CAGR Q3 |
|---|---|---|---|---|---|---|---|
| 8-10 лет | 7 | $32.54 | 93 | −42.9% | −7.9% | **−4.5%** | −2.1% |
| 6-8 | 7 | $2.45 | 845 | +81.3% | +4.0% | +7.9% | +30.3% |
| 4-6 | 9 | $0.42 | 16 147 | −8.4% | −4.0% | **+11.8%** | +13.6% |
| 2-4 | 18 | $0.32 | 39 936 | −25.5% | −23.1% | +4.4% | +20.2% |
| 0-2 | 12 | $0.34 | 66 988 | +623.7% | −51.8% | −38.9% | +52.3% |

Цена растёт с возрастом монотонно, оборот монотонно падает. **Перелом между 4-6
и 6-8 годами**: цена ×6, оборот ÷19. Но покупать самое старое поздно — на 8-10
годах CAGR уже отрицательный, а оборот 93 штуки в месяц.

Кривая по стикерам считается **отдельно**, не наследуется от капсул (стикеры
горят с первого дня, капсулы просто лежат). Данных пока мало.

## 4. ГЛАВНОЕ РАУНДА: предыдущий вывод был артефактом метрики

**Что я вывел сначала.** Ни один стикер из топ-30 по CAGR не проходил порог
500 штук/мес в Steam, а капсулы Berlin 2019 и Antwerp 2022 проходили с запасом
при CAGR +19…+44%. Вывод: «стикеры растут, но непокупаемы → брать капсулы».

**Почему это неверно.** Ликвидность мерялась только оборотом Steam. Но Steam —
худшее место для продажи дорогого: ~13% комиссии и кошелёк, который не
обналичивается. Дешёвую капсулу за $0.42 там продают (комиссия копеечная, баланс
уходит на игры), дорогой holo или автограф — на LIS / CSFloat / Buff за живые
деньги. Смещение **системное и направленное**: метрика завышает ликвидность
дешёвого класса и занижает у дорогого. Ровно та ось, по которой я строил вывод.

**Замер.** `lis.py` — свой синхронный клиент к `api.lis-skins.com/v1`, свой
рейт-лимитер 2 с/запрос, ключ читается из `/opt/vulpes/.env`. Сервис vulpes и
его БД не трогались (и он в любом случае остановлен по отдельной просьбе).

Первый контрольный предмет ломает исходный вывод сразу:

| предмет | Steam vol 30d | Steam цена | LIS лотов | LIS медиана | LIS/Steam |
|---|---|---|---|---|---|
| Sticker \| AdreN (Gold) \| Krakow 2017 | **1** | $1 626.07 | **12** | $639.16 | **0.39** |
| Sticker \| Astralis (Holo) \| Krakow 2017 | 25 | $12.04 | 0 | — | — |
| Antwerp 2022 Challengers Sticker Capsule | 16 147 | $0.42 | 1 200+ | $0.42 | 1.00 |

Одна сделка в месяц в Steam против 12 лотов в стакане вне его — это не
«непокупаемо», это «торгуется не там, где я смотрел».

Цена вне Steam ниже: 0.39 от Steam на этом предмете. Заказчик мерил 0.66-0.70 на
скинах; здесь ниже, но это одна точка.

**Появилась третья гипотеза, которой не было ни у кого.** Дорогие *капсулы* тоже
имеют тонкий стакан вне Steam: Boston 2018 Minor Challengers Autograph — 3 лота
при медиане $560.80, Boston 2018 Legends Autograph — 9 лотов при $223.73. Если
устоит, ось не «капсула против стикера», а **цена**: дорогое тонко торгуется
везде, дешёвое глубоко везде. Тогда и мой исходный вывод, и поправка заказчика —
частные случаи одного ценового эффекта. Проверяется таблицей «отношение
LIS-стакан / Steam-поток по ценовым полосам», она написана и ждёт данных.

## 5. Ликвидность разнесена на две колонки, навсегда

`steam_volume_30d` и `offsteam_listings` больше не сводятся в одну «ликвидность».
Это разные величины: **поток** (продано за месяц) и **запас** (лежит в стакане
сейчас). Усреднять их бессмысленно.

Вердикт в итоговой таблице теперь четырёхзначный, а не булев:
`liquid both` / `trades OFF Steam` / `Steam-only` / `illiquid both venues`.

Цензурирование помечается: `1 200+` — это потолок моей пагинации (6 страниц),
а не реальное число. В сравнениях это **занижает** ratio для дешёвого класса,
то есть работает против моего исходного вывода, а не за него.

## 6. Смещение отбора архива — измеряется, а не предполагается

Internet Archive хранит то, на что смотрят. Дорогие автографы попадают в архив
именно поэтому, значит любая статистика по «стикерам, для которых нашлась
история» смещена в сторону заметных. Это меряется долей покрытия по вариантам
(секция 0 в `sticker_analysis.py`), а не отмахивается.

Прямое подтверждение уже есть: `Sticker | Fnatic (Gold) | Boston 2018` —
`no captures`. Редкие варианты часто отсутствуют в архиве вообще. На текущий
момент 31 капча против 31 промаха, то есть половина целей архивом не покрыта.

Все CAGR показываются с квартилями и с оборотом рядом, чтобы число, стоящее на
пяти сделках в месяц, было видно как таковое.

## 7. Состояние на момент передачи

| | |
|---|---|
| предметов в карте | 129 (капсулы 84, кейсы 38, стикеры 5, прочее 2) |
| предметов с дневной историей | 155 |
| замеров вне Steam | 80 из 89, у 79 есть листинги |
| стикеров Wayback | 31 из 304, ещё 31 без покрытия, сбор идёт |
| запросов к LIS | 264, ни одного 429 |

Второй проход LIS — по всем 304 стикерам сетки, включая те, у которых Wayback
ничего не нашёл. Это отделит «нет в архиве» от «не торгуется»: если истории нет,
а стакан живой — виноват архив, а не рынок.

## 8. Что по-прежнему закрыто

* **Steam отдаёт 429 седьмой час.** Инвентаризация не собрана, `sell_listings`,
  `volume_24h`, `months_supply` пустуют. Один вежливый пробник в 18:38 — 429.
* **`supply_trend` пуст.** Cron стоит на 03:17, поставлен сегодня в 12:43,
  первый запуск — ночью. Не «второй снапшот не отработал», а первого ещё не было.
* **Привязка стикер → конкретная капсула-источник** невозможна: она живёт в
  фасете Steam `StickerCapsule`. Сравнение идёт на уровне турнира — это и есть
  уровень, на котором принимается решение «брать капсулы Krakow или стикеры
  Krakow». Выводить привязку из имён — выдумывание.

## 9. Правила, не изменившиеся

Steam-сессия не используется. Обходов защит нет, 429 пересиживается. vulpes не
трогается — из него только прочитан API-ключ и клиент; сам сервис остановлен по
отдельной просьбе заказчика, его БД целы, `PRAGMA integrity_check` = ok.
Метаданные не выдумываются: чего нет в источнике — помечается `unknown`.
